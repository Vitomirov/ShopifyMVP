services:
  backend:
    build: ./backend
    container_name: shopifymvp-backend
    ports:
      - "3001:3001"
    volumes:
      - ./backend:/app
      - ./backend/.env:/app/.env
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3001
    command: nodemon src/index.js
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: shopifymvp-frontend
    ports:
      - "5174:5174"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - VITE_BACKEND_URL=http://localhost:3001
    command: npm run dev
    restart: unless-stopped
